use_frameworks!

platform :ios, '8.0'

target 'TYSMBackgroundTask_Example' do
  pod 'TYSMBackgroundTask', :path => '../'

  target 'TYSMBackgroundTask_Tests' do
    inherit! :search_paths

    
  end
end
